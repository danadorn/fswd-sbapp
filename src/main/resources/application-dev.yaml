server:
  port: 8090
spring:
  application:
    name: fswd-sbapp
  datasource:
    url: jdbc:postgresql://localhost:5434/production_db
    username: danadorn
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true